<UserControl
    x:Class="BardMusicPlayer.Ui.Views.Dialogue.DialogueView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

    xmlns:auto="clr-namespace:BardMusicPlayer.Ui.Controls.AutoGrid"
    xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"

    xmlns:dialogue="clr-namespace:BardMusicPlayer.Ui.ViewModels.Dialogue"
    xmlns:s="https://github.com/canton7/Stylet"
    mc:Ignorable="d" d:DataContext="{d:DesignInstance dialogue:DialogueViewModel}">

    <md:Card Padding="24">
        <auto:StackPanel Orientation="Vertical" MarginBetweenChildren="24">
            <ItemsControl ItemsSource="{Binding Inputs}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ContentControl s:View.Model="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <auto:StackPanel Orientation="Horizontal" MarginBetweenChildren="12">
                <Button Content="Cancel" />
                <Button Content="Confirm" />
            </auto:StackPanel>
        </auto:StackPanel>
    </md:Card>
</UserControl>